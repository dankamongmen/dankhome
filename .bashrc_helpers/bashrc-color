if [ -r $HOME/.pcolor ] ; then
	PCOLOR=`cat $HOME/.pcolor`
else
	PCOLOR=white
fi

if [ -x "$HOME/bin/color" ] ; then
	export PS1=`color -p $PCOLOR`'[\h]($?) \$'`color -p none`' '
else
	export PS1='[\h]($?) \$ '
fi

unset PCOLOR

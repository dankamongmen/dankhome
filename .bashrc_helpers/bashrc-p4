if [ -r $HOME/local/p4/bin ] ; then
	if [ -n "$PATH" ] ; then
		export PATH=$PATH:$HOME/local/p4/bin
	else
		export PATH=$HOME/local/p4/bin
	fi
	export P4RCACHE=$HOME/tmp/p4rcache
	export P4CONFIG=.p4rc
fi

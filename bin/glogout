#!/bin/sh

set -e

GSS="`which gnome-session-save`" || echo "No gnome-session utilitites..."

if [ -x "$GSS" ] ; then
	#"$GSS" --kill --silent
	"$GSS" --logout-dialog --gui
#	exit 1
#else
fi
	sudo kill `pidof X`
#fi

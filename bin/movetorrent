#!/bin/sh

set -e
set -o nounset

usage () { echo "usage: `basename $0` downloaddir finaldir" ; return 0 ; }
usageerr () { usage >&2 ; return 1 ; }

[ $# -eq 2 ] || usageerr
[ -d "$1" ] || usageerr
[ ! -e "$2" ] || usageerr

mv -- "$1" "$2"
ln -sv -- "$2" "$1"
